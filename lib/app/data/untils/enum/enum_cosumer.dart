//changepass
enum RuleChangePass {
  ruleLeng,
  ruleLowCase,
  ruleUpperCase,
  ruleHaveNumber,
  ruleSpecialCharacter,
}

enum CheckValid { invalid, valid, uncheck }

//home
enum FilterHome { waiting, approved, refuse }

enum ErrorValidate { errorLength, errorFormat, errorEmpty }
